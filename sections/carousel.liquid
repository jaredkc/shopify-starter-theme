{%- liquid
  assign carousel_id = 'carousel-' | append: section.id
-%}

{% capture slides %}
  {% content_for 'blocks' %}
{% endcapture %}

<div
  class="spacing-style color-{{ section.settings.color_scheme }}"
  style="{% render 'spacing-style', settings: section.settings %}"
>
  {% render 'carousel',
    slides: slides,
    id: carousel_id,
    gap: section.settings.gap,
    slide_set_max_width: section.settings.slide_set_max_width,
    slide_max_width: section.settings.slide_max_width,
    slide_mobile_columns: section.settings.slide_mobile_columns,
    slide_desktop_columns: section.settings.slide_desktop_columns,
    track_padding: section.settings.track_padding,
    track_width: section.settings.track_width
  %}
</div>

{% schema %}
{
  "name": "t:names.carousel",
  "blocks": [
    {
      "type": "_carousel-slide"
    }
  ],
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "header",
      "content": "t:content.track"
    },
    {
      "id": "track_width",
      "type": "select",
      "label": "t:settings.max_width",
      "options": [
        { "value": "full", "label": "t:options.full" },
        { "value": "xl", "label": "t:options.xl" }
      ],
      "default": "full"
    },
    {
      "id": "track_padding",
      "type": "checkbox",
      "label": "t:settings.padding",
      "default": true
    },
    {
      "type": "header",
      "content": "t:content.slides"
    },
    {
      "type": "range",
      "id": "slide_mobile_columns",
      "label": "t:settings.mobile_columns",
      "min": 1,
      "max": 4,
      "step": 1,
      "default": 1
    },
    {
      "type": "range",
      "id": "slide_desktop_columns",
      "label": "t:settings.desktop_columns",
      "min": 1,
      "max": 12,
      "step": 1,
      "default": 2
    },
    {
      "id": "gap",
      "type": "range",
      "min": 0,
      "max": 60,
      "step": 4,
      "unit": "px",
      "label": "t:settings.gap",
      "default": 16
    },
    {
      "type": "checkbox",
      "id": "slide_set_max_width",
      "label": "t:settings.custom_width",
      "default": false
    },
    {
      "id": "slide_max_width",
      "type": "range",
      "min": 160,
      "max": 960,
      "step": 16,
      "unit": "px",
      "label": "t:settings.max_width",
      "default": 560,
      "visible_if": "{{ section.settings.slide_set_max_width == true }}"
    },
    {
      "type": "header",
      "content": "t:content.appearance"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:settings.color_scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:content.padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.top",
      "min": 0,
      "max": 104,
      "step": 8,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.bottom",
      "min": 0,
      "max": 104,
      "step": 8,
      "unit": "px",
      "default": 48
    }
  ],
  "presets": [
    {
      "name": "t:names.carousel",
      "category": "t:categories.banners",
      "blocks": [
        {
          "type": "_carousel-slide",
          "blocks": [
            {
              "type": "image",
              "settings": {
                "aspect_ratio": "16/9"
              }
            }
          ]
        },
        {
          "type": "_carousel-slide",
          "blocks": [
            {
              "type": "image",
              "settings": {
                "aspect_ratio": "16/9"
              }
            }
          ]
        },
        {
          "type": "_carousel-slide",
          "blocks": [
            {
              "type": "image",
              "settings": {
                "aspect_ratio": "16/9"
              }
            }
          ]
        }
      ]
    }
  ]
}
{% endschema %}
