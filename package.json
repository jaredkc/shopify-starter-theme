{
  "name": "shopify-starter-theme",
  "version": "1.0.0",
  "description": "Using TailwindCSS in your Shopify theme",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tailwindcss -i ./assets/tailwind.css -o ./assets/tailwind.min.css -m -w",
    "build": "npx tailwindcss -i ./assets/tailwind.css -o ./assets/tailwind.min.css -m && npx tailwindcss -i ./assets/tailwind.css -o ./assets/tailwind.build.css"
  },
  "author": "Jared Cornwall (https://github.com/jaredkc)",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.7",
    "tailwindcss": "^3.4.4"
  }
}
