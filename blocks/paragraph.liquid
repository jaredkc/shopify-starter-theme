<p class="m-0 {{ block.settings.font_size }} content-{{ block.settings.max_width }}" {{ block.shopify_attributes }}>
  {{ block.settings.text }}
</p>

{% schema %}
{
  "name": "t:names.paragraph",
  "tag": null,
  "settings": [
    {
      "type": "inline_richtext",
      "id": "text",
      "label": "t:settings.text",
      "default": "Insert your catchphrase here (or something else)."
    },
    {
      "type": "select",
      "id": "font_size",
      "label": "t:options.font_size",
      "options": [
        {
          "value": "text-sm",
          "label": "t:options.sm"
        },
        {
          "value": "text-base",
          "label": "t:options.md"
        },
        {
          "value": "text-lg",
          "label": "t:options.lg"
        },
        {
          "value": "text-xl",
          "label": "t:options.xl"
        }
      ],
      "default": "text-lg"
    },
    {
      "type": "select",
      "id": "max_width",
      "label": "t:settings.max_width",
      "options": [
        {
          "value": "xs",
          "label": "t:options.xs"
        },
        {
          "value": "sm",
          "label": "t:options.sm"
        },
        {
          "value": "md",
          "label": "t:options.md"
        },
        {
          "value": "lg",
          "label": "t:options.lg"
        },
        {
          "value": "full",
          "label": "t:options.full"
        }
      ],
      "default": "md"
    }
  ],
  "presets": [
    {
      "name": "t:names.paragraph"
    }
  ]
}
{% endschema %}
